# ============================================================
# Dependências do Projeto: Arbovirose Dengue DF
# Modelagem preditiva de surtos por Região Administrativa
# ============================================================

# --- Manipulação e análise de dados ---
pandas>=2.0.0
numpy>=1.24.0

# --- Machine Learning (scikit-learn + modelos de boosting) ---
scikit-learn>=1.3.0
xgboost>=2.0.0

# --- Previsão de Séries Temporais Hierárquicas ---
statsforecast>=1.7.0
mlforecast>=0.13.0
hierarchicalforecast>=0.4.0
utilsforecast>=0.1.0

# --- Redes Neurais (LSTM e deep learning) ---
tensorflow>=2.14.0          # ou torch>=2.1.0 se preferir PyTorch
# torch>=2.1.0              # descomente e comente o tensorflow acima para usar PyTorch

# --- Visualização ---
matplotlib>=3.7.0
seaborn>=0.13.0

# --- Requisições HTTP (Open-Meteo API de clima) ---
requests>=2.31.0

# --- Jupyter Notebook (para dengue.ipynb) ---
jupyter>=1.0.0
ipykernel>=6.25.0

# --- Utilitários ---
tqdm>=4.66.0                # barras de progresso
python-dotenv>=1.0.0        # variáveis de ambiente (opcional)

# --- Processamento de Documentos ---
markitdown[pdf]             # Extração de PDFs e conversão para Markdown
